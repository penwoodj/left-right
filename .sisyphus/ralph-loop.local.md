---
active: true
iteration: 1
max_iterations: 100
completion_promise: "DONE"
initial_completion_promise: "DONE"
started_at: "2026-06-02T04:02:12.018Z"
session_id: "ses_19eb9dcc6ffexWw8LUJ2yGOSVc"
strategy: "continue"
message_count_at_start: 3749
---
$?+ and $?- and !! operators should not exist 

we only have `` strings and comments are ``` at the end of a linewith no block comments 

{a:1} @& [`a`] is pluck or pick in functional programming so this would result in {a:1} as you are getting an object only with the keys and again only `` strings exist
 
get rid of Error[] as the !!! operator just throws the contents of whatever is left into the map containing the stack trace and a error key containing whatever is passed in on the left.

 We should not have the Class type system accessible in the language. We only have Operators Maps Lists Strings numbers booleans and undefined. Those are the only types that should exist and to be accessible within the language. When using a JavaScript library We just have paths to how we can to how we convert types. Knowles get turned you get turned into undefined's. All numbers get turned into the number type. All strings get turned into strings all arrays get turned into lists all objects could turn into maps and all of a sudden and all functions get turned into operators. functions that come from libraries Maintain the same name and import export collection structure So when you access an operator You use the variable name with spaces around it.  This is how I've always wanted it to work So first start by updating the documentation with this information and this and then get to fixing everything else as described and after you're done fixing everything as described Then walk me through each and every operator we have with multiple choice questions in batches So I can clarify what each and every operator should do based on the types that surround it.

walk through with good multiple choice questions each operator and what it does and how it responds to different types after you fix each of the above problems that still exist.   use opencode todos and plan files to keep track of your work and at the end update all live documentation and archive old docs not worth updating and keeping live. update unit integration and e2e tests as you go and implement incrementally and commit and push as you go to keep your progress.
